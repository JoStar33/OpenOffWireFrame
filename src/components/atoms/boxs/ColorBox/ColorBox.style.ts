import { StyleSheet } from "react-native";

const colorBoxStyle = StyleSheet.create({
  container: {
    marginTop: 10,
    marginRight: 10,
    paddingHorizontal: 10,
    paddingVertical: 3,
    borderRadius: 10,
    justifyContent: "center",
    alignItems: "center",
    textAlign: "center",
  }
});

export { colorBoxStyle };

