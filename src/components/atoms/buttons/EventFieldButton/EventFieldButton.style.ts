import { StyleSheet } from "react-native";

const eventFieldButton = StyleSheet.create({
  container: {
    maxWidth: 188,
    paddingHorizontal: 20,
    paddingVertical: 4,
    borderRadius: 20,
    justifyContent: "center",
    alignItems: "center"
  }
});

export { eventFieldButton };
