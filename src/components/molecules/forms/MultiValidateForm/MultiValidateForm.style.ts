import { StyleSheet } from "react-native";
import { vw } from "rxn-units";

export const multiValidateFormStyles = StyleSheet.create({
  container: {
    justifyContent: "center",
    alignItems: "center",
    flexDirection: "column",
  },
  buttonContainer: {
    marginTop: 20
  }
});
