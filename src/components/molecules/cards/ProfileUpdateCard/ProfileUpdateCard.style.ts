import { StyleSheet } from "react-native";
import { vw } from "rxn-units";

export const profileUpdateCardStyles = StyleSheet.create({
  cardTitle: {
    color: "black",
    fontSize: 17
  },
  container: {
    borderRadius: 10,
    backgroundColor: "#D9D9D9",
    padding: 10
  }
});