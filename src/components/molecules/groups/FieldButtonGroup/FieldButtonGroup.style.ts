import { StyleSheet } from 'react-native';

export const fieldButtonGroupStyles = StyleSheet.create({
  container: {
    flexDirection: "column",
    marginTop: 20
  },
  fieldContainer: {
    width: 360,
    flexWrap: "wrap",
    flexDirection: "row"
  },
  title: {
    fontSize: 12,
    color: "#FFF",
    width: 360
  }
});