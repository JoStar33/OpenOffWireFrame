import { StyleSheet } from "react-native";

export const eventMakeFormStyles = StyleSheet.create({
  container: {
    padding: 10,
    flexDirection: "column",
    flex: 1,
    backgroundColor: "black"
  },
  title: {
    color: "white",
    fontSize: 18
  }
});