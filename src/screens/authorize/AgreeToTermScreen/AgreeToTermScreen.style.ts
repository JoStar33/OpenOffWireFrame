import { StyleSheet } from 'react-native';
export const agreeToTermScreenStyles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    flexDirection: "column",
    backgroundColor: "black",
    position: "relative"
  },
  titleText: {
    width: 300,
    height: 90,
    fontSize: 25,
    color: "#FFF",
    marginBottom: 40
  }
});